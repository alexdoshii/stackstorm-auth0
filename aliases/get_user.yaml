name: "get_user"
pack: "auth0"
action_ref: "auth0.get_user"
description: "Get a user."
formats:
  - display: auth0 user get {{ user_id }}
    representation:
      - auth0 user get {{ user_id }}
ack:
  format: "Getting user from auth0"
  enabled: true
  append_url: false
result:
  format: "Here are the details: ```{{ execution.result.result }}```"
